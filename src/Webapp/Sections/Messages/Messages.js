import React from 'react';
import styled from 'styled-components';

const Canvas = styled.div`
  max-width: 1080px;
  margin: auto;
`;

const Messages = () => {
  return <Canvas>Messages</Canvas>;
};

export default Messages;
