import React from 'react';
import styled from 'styled-components';

const Canvas = styled.div`
  max-width: 1080px;
  margin: auto;
`;

const Calendar = () => {
  return <Canvas>calendar</Canvas>;
};

export default Calendar;
